Bootstrap: docker
From: ubuntu:24.04

%post
    apt-get update && \
    apt-get install -y --no-install-recommends \
        g++ \
        cmake \
        ninja-build \
        python3 \
        git \
        zlib1g-dev && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*
